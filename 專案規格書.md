# 細胞偵測系統專案規格書

## 快速入門指南

### 我要標記細胞 → 執行 `A3.py`
```bash
1. 將圖片放入 start/ 資料夾
2. 執行: python A3.py
3. 查看結果: finish/ 資料夾
```

### 我要訓練模型 → 執行 `train_DB.py`
```bash
1. 準備標註資料放入 DB/ 資料夾
2. 執行: python train_DB.py
3. 等待訓練完成（5-15 分鐘）
```

### 我的資料庫在哪？
**DB/** 資料夾 = 模型的知識庫（訓練資料集）
- `DB/images/train/` = 訓練圖片
- `DB/labels/train/` = 訓練標註檔

---

## 1. 專案概述

### 1.1 專案名稱
**細胞偵測系統 (Cell Detection System)**

### 1.2 專案目的
使用深度學習技術（YOLO v8）自動偵測圖片中的細胞（小白點），並在圖片上標記偵測結果，同時產生 YOLO 格式的標註檔案。

### 1.3 應用場景
- 醫學影像分析
- 細胞計數
- 生物樣本檢測
- 顯微鏡影像處理

### 1.4 技術架構
- **深度學習框架**: Ultralytics YOLO v8
- **影像處理**: OpenCV (cv2)
- **程式語言**: Python 3.x
- **訓練方式**: 遷移學習 (Transfer Learning)

---

## 2. 系統架構

### 2.1 系統組成
```
細胞偵測系統
├── 資料準備模組
│   ├── 圖片收集
│   ├── 標註工具 (anylabeling)
│   └── 格式轉換 (JSON → YOLO TXT)
│
├── 模型訓練模組
│   ├── 資料集配置 (DB_dataset.yaml)
│   ├── 訓練腳本 (train_DB.py)
│   └── 模型儲存 (runs/detect/)
│
└── 推理標記模組
    ├── 模型載入
    ├── 圖片推理
    ├── 結果標記
    └── 檔案輸出
```

### 2.2 工作流程
```
1. 資料準備
   ├── 使用 anylabeling 標註圖片（產生 JSON）
   ├── 使用 txtt.py 轉換格式（JSON → TXT）
   └── 整理到 DB 資料集
   ↓
2. 模型訓練 (train_DB.py)
   ├── 使用 DB 資料集訓練模型
   └── 儲存模型到 runs/detect/
   ↓
3. 模型推理 (A3.py)
   ├── 從 start 資料夾讀取圖片
   ├── 使用 DB 訓練的模型進行偵測
   └── 輸出到 finish 資料夾
```

---

## 3. 檔案結構

### 3.1 專案目錄
```
BB/
├── A3.py                    # 主要推理程式（標記細胞）
├── train_DB.py              # 模型訓練程式
├── DB_dataset.yaml          # 資料集設定檔
├── txtt.py                  # JSON 轉 YOLO TXT 工具
│
├── DB/                      # 訓練資料集（模型的知識庫）
│   ├── images/
│   │   ├── train/          # 訓練圖片
│   │   └── val/            # 驗證圖片（可選）
│   └── labels/
│       ├── train/          # 訓練標註檔（.txt）
│       └── val/            # 驗證標註檔（.txt）
│
├── start/                   # 輸入資料夾（A3.py）
│   └── *.jpg, *.png        # 待標記的圖片
│
├── finish/                  # 輸出資料夾（A3.py）
│   ├── detected_*.jpg      # 標記後的圖片
│   └── *_detected.txt      # 生成的標註檔
│
├── T1/                      # 輸入資料夾（txtt.py）
│   ├── *.jpg               # 圖片檔案
│   └── *.json              # JSON 標註檔
│
├── T2/                      # 輸出資料夾（txtt.py）
│   └── *.txt               # 轉換後的 YOLO TXT 標註檔
│
└── runs/detect/            # 訓練結果
    ├── DB_cell_detection2/
    ├── DB_cell_detection3/  # 目前使用的模型
    ├── DB_cell_detection4/
    └── DB_cell_detection5/
        └── weights/
            ├── best.pt     # 最佳模型
            └── last.pt     # 最後檢查點
```

### 3.2 核心檔案說明

#### 核心程式比較表

| 程式 | 作用 | 輸入 | 輸出 | 使用時機 |
|------|------|------|------|----------|
| **A3.py** | 標記細胞 | start/ 中的圖片 | finish/ 中的標記圖片 | 每次要標記新圖片 |
| **train_DB.py** | 訓練模型 | DB/ 中的訓練資料 | 模型檔案 (best.pt) | 改進模型、增加訓練資料 |
| **txtt.py** | 格式轉換 | T1/ 中的 JSON | T2/ 中的 TXT | 準備訓練資料時 |

#### 3.2.1 A3.py - 推理程式
- **功能**: 使用訓練好的模型對圖片進行細胞偵測和標記
- **模型知識庫**: `DB/` 資料夾（訓練資料集）
- **輸入**: `start/` 資料夾中的圖片
- **輸出**: `finish/` 資料夾
  - `detected_*.jpg`: 標記後的圖片
  - `*_detected.txt`: YOLO 格式標註檔
  - 原始圖片保留在 `start/` 資料夾（不移動）
- **主要參數**:
  - `conf_threshold = 0.15`: 信心度閾值
  - `min_area = 30`: 最小細胞面積（像素）
  - `max_area = 圖片面積 * 0.5`: 最大細胞面積
  - `min_confidence = 0.1`: 二次過濾信心度

#### 3.2.2 train_DB.py - 訓練程式
- **功能**: 訓練自訂的 YOLO v8 細胞偵測模型
- **輸入**: `DB/` 資料集中的圖片和標註檔
- **輸出**: `runs/detect/DB_cell_detection*/weights/best.pt`
- **訓練參數**:
  - `epochs = 50`: 訓練輪數
  - `batch = 16`: 批次大小
  - `imgsz = 640`: 圖片大小
  - `patience = 25`: 早停耐心值

#### 3.2.3 txtt.py - 格式轉換工具
- **功能**: 將 anylabeling 產生的 JSON 標註檔轉換為 YOLO 格式的 TXT
- **輸入**: `T1/` 資料夾中的 JPG 和 JSON 檔案
- **輸出**: `T2/` 資料夾中的 TXT 檔案
- **處理流程**:
  1. 從 `T1/` 讀取 JPG 檔案
  2. 尋找對應的 JSON 標註檔（同檔名）
  3. 轉換為 YOLO 格式的 TXT
  4. 儲存到 `T2/` 資料夾

#### 3.2.4 DB_dataset.yaml - 資料集設定
- **功能**: 定義資料集路徑和類別資訊
- **內容**:
  - 資料集路徑
  - 訓練/驗證圖片路徑
  - 類別名稱和數量

---

## 4. 資料格式

### 4.1 圖片格式
- **支援格式**: `.jpg`, `.jpeg`, `.png`
- **建議大小**: 640x640 或更大
- **色彩空間**: RGB

### 4.2 標註格式 (YOLO TXT)
每行格式：
```
class_id center_x center_y width height
```
- **class_id**: 類別 ID（本專案為 0，代表細胞）
- **center_x, center_y**: 歸一化的中心點座標 (0-1)
- **width, height**: 歸一化的寬度和高度 (0-1)

**範例**:
```
0 0.512345 0.678901 0.123456 0.098765
```

### 4.3 標註工具
- **anylabeling**: 產生 JSON 格式標註
- **txtt.py**: 將 JSON 轉換為 YOLO TXT 格式

---

## 5. 功能說明

### 5.1 模型訓練功能

#### 5.1.1 訓練流程
1. 檢查資料集是否存在
2. 驗證圖片和標註檔數量是否一致
3. 載入預訓練模型 (`yolov8n.pt`)
4. 開始訓練（遷移學習）
5. 自動儲存最佳模型

#### 5.1.2 訓練輸出
- **best.pt**: 驗證集上表現最好的模型
- **last.pt**: 最後一個訓練檢查點
- **results.csv**: 訓練過程指標
- ***.png**: 訓練曲線圖（Precision, Recall, mAP 等）

#### 5.1.3 訓練指標
- **mAP50**: 在 IoU=0.5 時的平均精度
- **mAP50-95**: 在 IoU=0.5-0.95 時的平均精度
- **Precision**: 精確度
- **Recall**: 召回率

### 5.2 推理標記功能

#### 5.2.1 推理流程
1. 載入使用 DB 資料集訓練的模型（`DB_cell_detection3/weights/best.pt`）
2. 讀取 `start/` 資料夾中的圖片
3. 過濾已標記的檔案（`detected_*`）
4. 對每張圖片進行推理
5. 過濾偵測結果（面積、信心度、座標驗證）
6. 在圖片上標記偵測到的細胞
7. 儲存標記後的圖片和標註檔到 `finish/` 資料夾
8. 原始圖片保留在 `start/` 資料夾（不移動）

#### 5.2.2 標記視覺化
- **綠色矩形框**: 細胞邊界框
- **紅色圓點**: 細胞中心點
- **白色文字**: 細胞編號和信心度
- **左下角統計**: 偵測到的細胞總數

#### 5.2.3 過濾機制
1. **信心度過濾**: 只保留信心度 ≥ 0.15 的偵測
2. **面積過濾**: 只保留面積在 30 像素到圖片 50% 之間的偵測
3. **座標驗證**: 確保邊界框在圖片範圍內
4. **二次信心度過濾**: 最終信心度 ≥ 0.1

---

## 6. 使用說明

### 6.0 兩個核心程式的差別

#### train_DB.py = 老師教學生（訓練模型）
- **作用**: 讓 AI 學習如何辨識細胞
- **輸入**: `DB/` 資料夾中已標記好的圖片和標註檔（教材）
- **輸出**: 訓練好的模型 `best.pt`（學習成果）
- **比喻**: 老師用課本教學生，學生學會後就能考試
- **使用頻率**: 很少用（可能幾天或幾週一次）

#### A3.py = 學生考試（使用模型）
- **作用**: 使用訓練好的模型來標記新圖片
- **輸入**: `start/` 資料夾中未標記的圖片
- **輸出**: 標記後的圖片和標註檔到 `finish/`
- **比喻**: 學生學會後，拿到新考卷就能答題
- **使用頻率**: 經常用（可能每天多次）

**為什麼要分開？**
- 訓練很慢（5-15 分鐘），但只需要做一次
- 推理很快（每張圖 1-2 秒），可以重複使用

### 6.1 環境需求
```python
# 必要套件
ultralytics >= 8.0.0
opencv-python >= 4.5.0
numpy >= 1.20.0
matplotlib >= 3.3.0
```

### 6.2 資料準備（給 train_DB.py 用）

**資料庫位置**: `DB/` 資料夾（這是模型的知識庫）

#### 方法 1: 直接準備 YOLO 格式資料

**步驟 1: 準備訓練圖片**
將標註好的圖片放入 `DB/images/train/`

**步驟 2: 準備標註檔**
將 YOLO 格式的標註檔（.txt）放入 `DB/labels/train/`
- 檔名需與圖片檔名一致（副檔名不同）
- 例如: `image.jpg` 對應 `image.txt`

**步驟 3: 準備驗證資料（可選）**
將驗證圖片和標註檔放入 `DB/images/val/` 和 `DB/labels/val/`

#### 方法 2: 使用 txtt.py 轉換格式

如果你使用 anylabeling 標註，會產生 JSON 檔案，需要先轉換為 YOLO TXT 格式：

**步驟 1: 準備 JSON 標註**
將圖片和 JSON 檔案放入 `T1/` 資料夾

**步驟 2: 執行轉換**
```bash
python txtt.py
```

**步驟 3: 移動到 DB**
將 `T2/` 中的 TXT 檔案移動到 `DB/labels/train/`，將圖片移動到 `DB/images/train/`

### 6.3 模型訓練

```bash
python train_DB.py
```

**訓練過程**:
- 顯示訓練進度
- 顯示 Loss 值和 mAP 值
- 自動儲存最佳模型

**訓練時間**: 約 5-15 分鐘（取決於資料量和硬體）

### 6.4 圖片標記

#### 步驟 1: 放置待標記圖片
將要標記的圖片放入 `start/` 資料夾

#### 步驟 2: 執行推理
```bash
python A3.py
```

#### 步驟 3: 查看結果
- 標記後的圖片: `finish/detected_*.jpg`
- 標註檔: `finish/*_detected.txt`
- 原始圖片: 保留在 `start/` 資料夾中

### 6.5 格式轉換（txtt.py）

#### 步驟 1: 準備檔案
將 JPG 圖片和對應的 JSON 標註檔放入 `T1/` 資料夾

#### 步驟 2: 執行轉換
```bash
python txtt.py
```

#### 步驟 3: 取得結果
- 轉換後的 TXT 檔案: `T2/*.txt`

---

## 7. 技術規格

### 7.1 模型規格
- **基礎模型**: YOLOv8n (Nano)
- **輸入尺寸**: 640x640 像素
- **輸出格式**: 邊界框座標 + 信心度
- **類別數量**: 1（細胞）

### 7.2 偵測參數

| 參數 | 數值 | 說明 |
|------|------|------|
| conf_threshold | 0.15 | YOLO 推理信心度閾值 |
| min_confidence | 0.1 | 二次過濾信心度閾值 |
| min_area | 30 | 最小細胞面積（像素²） |
| max_area | 圖片面積 × 0.5 | 最大細胞面積（像素²） |

### 7.3 訓練參數

| 參數 | 數值 | 說明 |
|------|------|------|
| epochs | 50 | 訓練輪數 |
| batch | 16 | 批次大小 |
| imgsz | 640 | 圖片大小 |
| patience | 25 | 早停耐心值 |

---

## 8. 模型管理

### 8.1 模型版本
目前固定使用：`DB_cell_detection3/weights/best.pt`

此模型是使用 `DB/` 資料集訓練的，作為模型的知識庫。

### 8.2 模型儲存位置
```
runs/detect/DB_cell_detection*/weights/
├── best.pt    # 最佳模型（用於推理）
└── last.pt    # 最後檢查點（用於繼續訓練）
```

---

## 9. 輸出格式

### 9.1 標記圖片格式
- **檔名**: `detected_原始檔名.jpg`
- **格式**: JPG
- **內容**: 原始圖片 + 綠色邊界框 + 紅色中心點 + 文字標籤

### 9.2 標註檔格式
- **檔名**: `原始檔名_detected.txt`
- **格式**: YOLO TXT
- **內容**: 每行一個細胞的標註資訊

---

## 10. 常見問題

### 10.0 基本問題

**Q: 我的資料庫在哪裡？**
- A: `DB/` 資料夾就是你的資料庫（訓練資料集）
  - `DB/images/train/` = 訓練圖片
  - `DB/labels/train/` = 訓練標註檔

**Q: 我要標記細胞應該用哪個程式？**
- A: 使用 `A3.py`
  1. 將圖片放入 `start/` 資料夾
  2. 執行 `python A3.py`
  3. 查看 `finish/` 資料夾的結果

**Q: A3.py 和 train_DB.py 有什麼差別？**
- A: 
  - `A3.py` = 標記細胞（經常用）
  - `train_DB.py` = 訓練模型（很少用）
  - 參考第 6.0 節的詳細說明

**Q: start 資料夾找不到圖片怎麼辦？**
- A: 確認圖片檔案已放入 `start/` 資料夾
  - 支援格式: `.jpg`, `.jpeg`, `.png`
  - 不要放已標記的圖片（`detected_*` 開頭）

### 10.1 訓練問題

**Q: 訓練時出現記憶體不足錯誤**
- A: 將 `batch` 參數改為 8 或 4

**Q: 訓練資料不足怎麼辦？**
- A: 建議至少 30-50 張標註好的圖片，目前只有 15 張

**Q: 如何提高模型準確度？**
- A: 
  1. 增加訓練資料
  2. 確保標註品質
  3. 調整訓練參數（epochs, batch size）
  4. 使用更大的模型（yolov8s, yolov8m）

### 10.2 推理問題

**Q: 有些細胞沒有被標記到**
- A: 
  1. 降低 `conf_threshold`（目前 0.15）
  2. 降低 `min_area`（目前 30）
  3. 重新訓練模型，增加訓練資料

**Q: 標記了太多誤判**
- A: 
  1. 提高 `conf_threshold`
  2. 提高 `min_confidence`
  3. 調整 `min_area` 和 `max_area`

**Q: 訓練集中的圖片沒有被標記到**
- A: 
  1. 檢查模型是否為最新版本
  2. 檢查過濾條件是否太嚴格
  3. 檢查模型訓練效果（mAP 值）

---

## 11. 未來改進方向

### 11.1 功能擴展
- [ ] 支援多類別細胞偵測
- [ ] 批次處理大量圖片
- [ ] Web 介面
- [ ] 即時影像處理

### 11.2 模型優化
- [ ] 使用更大的模型（yolov8s, yolov8m）
- [ ] 資料增強（Data Augmentation）
- [ ] 模型蒸餾（Model Distillation）

### 11.3 資料處理
- [ ] 自動標註工具
- [ ] 標註品質檢查
- [ ] 資料集平衡

---

## 12. 版本歷史

### v1.2 (當前版本)
- 新增快速入門指南
- 新增核心程式比較表
- 新增資料庫位置說明
- 新增 train_DB.py 與 A3.py 的詳細差異說明
- 新增常見基本問題解答
- 改善資料準備說明（含 txtt.py 流程）

### v1.1
- 基本細胞偵測功能
- YOLO v8 模型訓練
- 自動標記和標註檔生成
- 使用 start/finish 資料夾流程
- JSON 轉 TXT 格式轉換工具（txtt.py）
- 原始圖片保留在 start 資料夾

### v1.0
- 基本細胞偵測功能
- YOLO v8 模型訓練
- 自動標記和標註檔生成

---

## 13. 聯絡資訊

如有問題或建議，請聯繫專案維護者。

---

**文件版本**: 1.2  
**最後更新**: 2024-11-26

